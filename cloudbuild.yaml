steps:
  - name: 'eclipse-temurin:25-jdk'
    entrypoint: './gradlew'
    args:
      - ':server:build'
      - '-PexcludeMobile=true'
      - '-x'
      - 'test'
      - '--build-cache'
      - '--no-daemon'
      - '--quiet'

options:
  logging: CLOUD_LOGGING_ONLY